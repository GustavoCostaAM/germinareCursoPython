semana 3: estruturas de condiçoes:

Estruturas simples:
if(condiçao):

Estruturas compostas:
if(condiçao):

else:

todos comandos que estarem colados na borda serao realizados independentes da condiçao.

texto.strip() ---> remove os espaços em branco de um string, sendo ele o valor retornado.

any() ---> verifica se pelo menos um dos valores dentro é verdadeiro

list(palavra) ---> separa em um array, todos os strings de algo

import nome biblioteca ---> importa uma biblioteca

from ---> seleciona algo de algo EX: from Math import randint

in ---> "EM" algo, retorna true  se é verdadeiro, falso se é falso.

random.random() ---> funcao que retorna um valor float aleatorio\

randint(de, até) retorna um numero inteiro aleatorio exceto se voce seleciona os limites dentro do parenteces\

range(de, até) ---> faz um range com limites que coloca dentro dos parenteces


for variavel in seleciona outro valor(ex: range(0, 5)):
    linhas de codigo

while(condiçao) ---> estrutura de repeticao, igual js